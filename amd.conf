/etc/asterisk/extensions.conf:; VICIDIAL_auto_dialer transfer script AMD with Load Balanced:
/etc/asterisk/extensions.conf:exten => 8369,n,AMD(5100,2000,1000,5000,120,50,5,256)



;
; Answering Machine Detection Configuration
;

[general]
initial_silence = 2500		; Maximum silence duration before the greeting.
				; If exceeded then MACHINE.
greeting = 1500			; Maximum length of a greeting. If exceeded then MACHINE.
after_greeting_silence = 800	; Silence after detecting a greeting.
				; If exceeded then HUMAN
total_analysis_time = 5000	; Maximum time allowed for the algorithm to decide
				; on a HUMAN or MACHINE
min_word_length = 200		; Minimum duration of Voice to considered as a word
between_words_silence = 50	; Minimum duration of silence after a word to consider
				; the audio what follows as a new word
maximum_number_of_words = 3	; Maximum number of words in the greeting.
				; If exceeded then MACHINE
silence_threshold = 256

